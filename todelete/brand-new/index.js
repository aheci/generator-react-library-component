import React from 'react'
import PropTypes from 'prop-types'
import './style.scss'

const propTypes = {
  
}

const defaultProps = {
  
}

export default function BrandNew(props) {
  return (
    <div className="brand-new">
      {/* Place your elements here */}
    </div>
  )
}

BrandNew.propTypes = propTypes;
BrandNew.defaultProps = defaultProps;